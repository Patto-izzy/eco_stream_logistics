# Eco-Stream Logistics — Student Project

Static multi-page site for the Eco-Stream Logistics brief.

Quick start

1. Open `index.html` in a browser (double-click) or run a local server:

```powershell
# from this project root
python -m http.server 8000
# then open http://localhost:8000
```

What you get

- `index.html`, `about.html`, `services.html`, `contact.html`
- `css/style.css` for responsive, accessible styling
- `js/main.js` for form handling (mock submit)
- `docs/` for investigation, design and testing notes
